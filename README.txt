# EntitySystem
Basic entity/component system for the Monkey2 language.

This a is very simple, zero dependency (only uses std module) entity/component system, mostly intended to be extended by more sophisticated and specialized classes.
